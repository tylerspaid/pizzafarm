The Pizza Farm Chaos Machine is a cloud-deployed, highly concurrent backend service designed to stress-test data integrity and failure handling.
It uses a headless polyglot architecture to demonstrate transfer and storage of data, back-end logic processing, and front-end automated inputs.

This service simulates a chaotic restaurant environment. It continuously fires randomized, simultaneous queries forcing each piece of the
architecture to respond to inputs whether valid or invalid.
